# Roadmap

## 1. 拆分 CI

目标是把包正确性、Notebook 可执行性、站点渲染和发布拆成边界清晰的工作流，避免 `dnnlpy` 的单独修改触发耗时约 15～30 分钟的全站 Notebook 重跑。

### 1.1 `dnnlpy` 包 CI

- [ ] 当 `dnnlpy/**`、包配置或测试依赖变化时，只运行包 CI，不自动触发完整 Quarto 渲染。
- [ ] 保留 Python 版本矩阵中的单元测试、数值正确性测试、模型与优化器的 PyTorch 对齐测试。
- [ ] Ruff、构建等与 Python 版本无关的检查只运行一次，仅将 pytest 放入多版本矩阵。
- [ ] 新增快速的 Notebook 契约测试，覆盖 Notebook 中真正由 `dnnlpy` 提供的关键调用链。
- [ ] 优先补充 `VAE → loss → backward → Adam.step()` 和 `MiniGPT → next-token loss → backward → AdamW.step()`，使用小模型、合成数据和一至两个训练步骤。
- [ ] 契约测试检查输出形状、有限 loss、有限梯度、参数更新以及必要的保存/加载行为，不要求模型训练至收敛。
- [ ] 不复制完整 Notebook、下载真实数据集或检查生成质量；不为主要使用 Hugging Face API 的 ViT Notebook 添加名不副实的 `dnnlpy` 契约测试。

### 1.2 Notebook 与站点 CI

- [ ] 当 `zh/**`、`en/**`、`cs336/**`、Quarto 配置、样式、参考文献或站点依赖变化时，运行站点 CI。
- [ ] 普通 Notebook 在站点 CI 中正常执行，继续承担导入、API 参数、数据形状和跨组件组合的端到端冒烟验证。
- [ ] 重型 Notebook 不再作为普通 PR 的训练任务；将选定的实验结果直接写入 `.qmd`，生成图片保存到 `figures/` 后引用，并禁用对应的昂贵执行单元或整个文档执行。
- [ ] 重型 Notebook 中记录结果来源，包括 `dnnlpy`/PyTorch 版本、随机种子、设备、训练步数等，明确其为版本化的实验快照。
- [ ] 重型实验在需要更新结果时由作者本地执行，或通过 `workflow_dispatch` 在合适的 runner 上手动执行；普通 PR 不要求复现实验。
- [ ] 站点 CI 必须完成全站结构、公式、图片、交叉引用和链接验证，但不能把静态实验结果表述为“已重新执行全部 Notebook”。

### 1.3 重型测试与完整验证

- [ ] 将“功能正确性”和“大规模性能验证”分开：用仓库内的小型确定性数据覆盖日常正确性，用真实数据集保留规模验证。
- [ ] 对测试使用明确标记，如 `integration`、`large_data`、`benchmark` 和 `gpu`，避免所有重型场景都归入含义模糊的 `slow`。
- [ ] PR 默认运行不依赖大数据、GPU 或基准测试的完整 CPU 正确性套件。
- [ ] TinyStories 等完整数据集测试改为定时、手动或发布前执行；该工作流应显式准备数据，数据缺失时失败，而不是静默跳过。
- [ ] 为重型任务设置超时，并将耗时和内存数据写入 Actions Summary；共享 runner 上的性能断言使用宽松阈值或稳定的自托管 runner。

### 1.4 触发与路由规则

| 修改范围 | 必须运行的检查 |
| --- | --- |
| 仅 `dnnlpy/**` | 包测试与 Notebook 契约测试 |
| 仅 Notebook 或站点配置 | 普通 Notebook 执行与完整站点渲染 |
| 同时修改包和 Notebook | 包 CI 与站点 CI |
| 有意修改公共 API 或文档输出 | 包 CI 与站点 CI，即使最初只修改包代码 |
| 重型实验结果更新 | 本地或手动重型工作流，然后提交更新后的文本、表格和图片 |
| 定时或发布前验证 | 大数据、基准测试、GPU 或完整重型实验，按需要执行 |

### 1.5 发布流程

- [ ] 合并 TestPyPI 和 PyPI 中重复的测试与构建步骤：测试一次、构建一次、签名或证明一次，再发布同一份产物。
- [ ] TestPyPI 改为手动触发或仅用于预发布版本，避免每次正式发布重复上传和验证。
- [ ] 检查 PyPI environment 的保护规则与等待计时器，避免实际发布只需数秒却等待约一小时。
- [ ] 复用依赖缓存和公共环境配置；继续缓存数据集，但优先优化真正耗时的 Notebook 执行。
- [ ] 保留并发取消，让同一 PR 的旧提交停止运行，减少无效 runner 时间。

### 1.6 迁移顺序与验收标准

- [ ] 先建立 VAE、MiniGPT 等关键 Notebook 契约测试，并确认重要的 `dnnlpy` 使用方式已有覆盖。
- [ ] 再从 `pr-render-check.yml` 和自动站点部署工作流的路径过滤中移除仅由 `dnnlpy/**` 引起的触发。
- [ ] 将重型 Notebook 的代表性结果和图片静态写入文档，并明确其手动更新方式。
- [ ] 最后合并发布流程、整理测试标记，并加入可选的定时/手动完整验证。
- [ ] 验收：包代码的单独修改能快速得到正确性反馈；文档修改仍验证完整站点；重型实验不会阻塞普通 PR；发布只测试和构建一次。

## 2. 第 5 章：卷积神经网络（CNN）

- [ ] 完善 CNN 基础内容，包括卷积、池化和下采样。
- [ ] 保留从零实现 `Conv2d` 和构建简单 CNN 的教学内容。
- [ ] 以 LeNet 作为经典 CNN 架构案例。
- [ ] 同步中文、英文目录以及 Quarto 导航配置。

## 3. 第 6 章：现代卷积神经网络

- [ ] 新增第 6 章，用于介绍现代 CNN 架构及其关键设计思想。
- [ ] 规划代表性模型，并突出从经典 CNN 到现代 CNN 的演进。
- [ ] 补充可运行的模型实现、训练示例和架构对比。
- [ ] 同步中文、英文目录以及 Quarto 导航配置。

## 4. 第 8 章：循环神经网络（RNN/LSTM）

- [ ] 将第 8 章调整为 RNN/LSTM，介绍序列建模的基本概念。
- [ ] 覆盖 RNN 的前向传播、隐藏状态和从零实现。
- [ ] 覆盖 LSTM 的门控机制、实现与训练示例。
- [ ] 补充 RNN、LSTM 的对比以及常见应用。

## 5. 第 9 章：注意力机制与 Transformer

- [ ] 将现有第 8 章的 Attention/Transformer 内容整体迁移到第 9 章。
- [ ] 调整章节目录、文件名、图片名和内部链接中的章节编号。
- [ ] 更新中英文导航、交叉引用和相关章节链接。
- [ ] 迁移后执行完整渲染，确认图片、公式、代码和链接均正常。

## 6. 可视化与 CS336 补全

- [ ] 为重点章节补充模型结构、数据流和训练过程的可视化内容。
- [ ] 统一图表的样式、配色、命名和中英文说明。
- [ ] 对照 CS336 课程要求，梳理现有实现并标记完整、部分支持和缺失内容。
- [ ] 补全缺失的 CS336 实现、测试和可运行示例。
- [ ] 完善 CS336 配置、实验入口和文档说明，确保示例可以复现。
